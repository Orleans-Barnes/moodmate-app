/**
 * Central app configuration.
 *
 * HOW TO UPDATE THE BACKEND IP
 * ─────────────────────────────
 * 1. On your PC, open a terminal and run:  ipconfig   (Windows)
 * 2. Find "IPv4 Address" under your active Wi-Fi adapter.
 * 3. Replace the IP below — keep the port :8080.
 *
 * This is the ONLY file you need to touch when your IP changes.
 */
export const BACKEND_BASE_URL = 'http://10.94.112.149:8080';
