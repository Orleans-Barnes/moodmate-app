target/
*.class
*.log
.env
.idea/
*.iml
.vscode/
.DS_Store
HELP.md

# start.bat carries real per-developer secrets (Groq key, Gmail app password) - was already
# supposed to be gitignored per its own header comment, but never actually was, which is how a
# real Groq key and Gmail app password ended up committed to local history once. Keep this local
# only; each developer sets their own values in their own uncommitted copy.
start.bat
