package com.moodmate.ai.client;

/** Local copy of moodmate-crisis's entity.CrisisSeverity - must stay in sync since it's
 * serialized by name over HTTP. */
public enum CrisisSeverity { CRITICAL, HIGH }
